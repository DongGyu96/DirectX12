D3DXVec3Lerp(선형보간) 리니어 인터폴레이션 (내분 외분)
2개의 벡터 사이의 임의의 점들을 구하는 것

스케일링을 사용하면 안되는 이유
역행렬의 전치행렬을 구해야 하기 때문
법선벡터는 수직 어떤 접선을 스케일링 할 경우 
	- 만약 x y z축의 크기 변환이 다 다를 경우 법선 벡터가 변환되어 
	접선하고 더이상 수직이 되지 않아  법선 벡터가 되지 않는다.
	- x y z축의 크기변환이 같으면 법선 벡터의 방향은 바뀌지 않지만
	더이상 단위 벡터가 되지 않기 때문에 단위 벡터로 만들어야 한다.

XMVECTOR 16바이트 메모리 정렬 필요
- 128비트짜리 자료형타입
XMMATRIX 는 지역변수 전역변수일경우 컴파일러가 자동으로 메모리 정렬을 해줌
그러나 클래스 또는 동적 메모리할당에 사용할경우 정렬을 해주지 않기때문에
( 소멸자 떄문? )
지역변수 전역변수 외에는 사용하지 말아야함 그러나
x64에서 모든 동적 메모리 할당은 16바이트로 정렬되기 때문에 
동적메모리할당은 가능 
결론은 클래스나 구조체의 멤버변수로는 사용해서는 안됨
또한 XMVECTOR는 구조체가 아닌 그자체 하나의 자료형 타입으로 내부 변수에 접근x
XMMATRIX는 XMVECTOR 4개가 달린 구조체